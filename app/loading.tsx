'use client';

import React from 'react';
import { PageTransition } from '@/components';

export default function Loading() {
  return (
    <PageTransition>
      Loading...
    </PageTransition>
  );
}
